---
title: "Modify prompt enhancements"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Modify prompt enhancements}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(chattr)
```

To edit what `chattr` is sending to the model you can use `chattr_defaults()`:

```{r}
chattr_defaults()
```


To modify, simply pass the new value as an argument to the function:

```{r}
chattr_defaults(prompt = c("New instructions", "New line"))
```
